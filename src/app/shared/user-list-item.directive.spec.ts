import { UserListItemDirective } from './user-list-item.directive';

describe('UserListItemDirective', () => {
  it('should create an instance', () => {
    const directive = new UserListItemDirective();
    expect(directive).toBeTruthy();
  });
});
